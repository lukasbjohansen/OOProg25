@page
@model RosBilRP.Pages.Opgaver.ForLejeModel
@{
}

<h1 class="display-3 mb-5">Opgaver for Leje (@Model.Element.Beskrivelse)</h1>

<div class="container">
    <div class="row">
        @foreach (var elem in Model.Data)
        {
            <div class="col-md-4 p-3">
                <div class="card shadow" style="width: 18rem;">
                    <div class="card-body d-flex flex-column">
                        <h3 class="card-title">@elem.Ansat.Navn</h3>
                        <h5>@elem.Beskrivelse</h5>
                        <h5>@(elem.Afsluttet ? "Afsluttet" : "I Gang")</h5>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
